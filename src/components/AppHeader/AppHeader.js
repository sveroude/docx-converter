import React from 'react';
import './AppHeader.css';

const AppHeader = () => (
    <header className="app-header">
        <h1 className="app-title">doc.x converter</h1>
    </header>
);

export default AppHeader;
